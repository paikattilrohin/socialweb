DROP TABLE LOGIN CASCADE CONSTRAINTS;
DROP TABLE Posts CASCADE CONSTRAINTS;

CREATE TABLE LOGIN(USERID VARCHAR(255) NOT NULL, email_ID VARCHAR(255) NOT NULL,password VARCHAR(25) NOT NULL ,CONSTRAINT LOGIN_PK PRIMARY KEY (USERID));

CREATE TABLE Posts(POST_ID VARCHAR(10),Title VARCHAR(255) NOT NULL, Content VARCHAR(255) NOT NULL, USERID VARCHAR(255), CONSTRAINT Posts_PK PRIMARY KEY (POST_ID) );

ALTER TABLE Posts ADD CONSTRAINT Posts_FK
FOREIGN KEY (UserID)
REFERENCES LOGIN (USERID);